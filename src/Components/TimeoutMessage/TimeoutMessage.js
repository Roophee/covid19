import React from 'react';
import StyledTimeoutMessage from './style';

const TimeoutMessage = () => (
  <StyledTimeoutMessage>
    Something went wrong. Please, reload the page or it try later.
  </StyledTimeoutMessage>
);

export default TimeoutMessage;
