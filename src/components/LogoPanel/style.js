import styled from 'styled-components';

const StyledLogoPanel = styled.div`
  display: flex;
  align-items: center;

  @media (max-width: 550px) {
    justify-content: center;
    align-items: center;
  }
`;

export default StyledLogoPanel;
