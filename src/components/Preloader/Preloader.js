import React from 'react';
import StyledPreloader from './style';

const Preloader = () => (
  <StyledPreloader className="loader">
    <div className="loader">loading</div>
  </StyledPreloader>
);

export default Preloader;
